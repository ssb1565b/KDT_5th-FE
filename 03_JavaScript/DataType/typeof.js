let num = 1;
let str = "hi";
let boo = true;
let nil = null;
let obj = {};

console.log(` "${typeof num}" isn't "${typeof str}" data type. `);
console.log(
  `typeof를 "${typeof boo}" 이나 "${nil}"에 사용하면, "${typeof obj}"결과를 얻을 수 있습니다.`
);
